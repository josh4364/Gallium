{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Gallium: Run",
            "type": "python",
            "request": "launch",
            "module": "source.main",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "justMyCode": false,
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
